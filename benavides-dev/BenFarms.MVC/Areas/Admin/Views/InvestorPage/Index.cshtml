@model IEnumerable<BenavidesFarm.DataModels.Models.Pages.InvestorPage>

@{
    ViewBag.Title = "Listado de Páginas de Inversionistas";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewBag.Page = "InvestorPageIndex";
}

<div class="portlet light bordered">
    <div class="portlet-title">
        <div class="caption font-dark">
            <i class="icon-settings font-dark"></i>
            <span class="caption-subject bold uppercase"> Listado Páginas de Inversionistas </span>
        </div>
    </div>
    <div class="portlet-body">
        <div class="table-toolbar">
            <div class="row">
                <div class="col-md-6">
                    <div class="btn-group">
                        <a href="~/Admin/InvestorPage/Edit" class="btn sbold green"><i class="fa fa-plus"></i> Editar Página </a>
                    </div>
                </div>
            </div>
        </div>
        <table class="table table-striped table-bordered table-hover order-column" id="sample_1">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.InvestorPageCustomValue)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.InvestorPageActive)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.InvestorPageCreatedDate)
                    </th>
                    <th>Vista Previa</th>
                    <th>Activar</th>
                    <th>Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.InvestorPageCustomValue)
                        </td>
                        <td>
                            @{
                                if (item.InvestorPageActive == true)
                                {
                                    <span class="label label-sm label-success"> Activa </span>
                                }
                                else
                                {
                                    <span class="label label-sm label-warning"> Desactivada </span>

                                }
                            }
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.InvestorPageCreatedDate)
                        </td>
                        <td>
                            @{ var mEdit = "~/Admin/InvestorPage/Preview/" + item.InvestorPageId;}
                            <a href="@Url.Content(mEdit)" target="_blank"><li class="glyphicon glyphicon-search " style="color:lawngreen"></li></a>
                        </td>
                        <td>
                            @{
                                var mAct = "~/Admin/InvestorPage/SetState/" + item.InvestorPageId;
                                if (!item.InvestorPageActive)
                                {
                                    <a href="@Url.Content(mAct)"><li class="fa fa-check" style="color:goldenrod">Activar</li></a>
                                }
                            }
                        </td>
                        <td>
                            @if (item.InvestorPageActive == false)
                            {
                                var deleteLink = "~/Admin/InvestorPage/ConfirmDeletion/" + item.InvestorPageId;
                                <a href="@Url.Content(deleteLink)"><div class="glyphicon glyphicon-trash" style="color: goldenrod"> Eliminar</div></a>
                            }
                        </td>
                    </tr>
                                }
            </tbody>
        </table>
    </div>
</div>