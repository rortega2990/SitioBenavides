@model IEnumerable<BenavidesFarm.DataModels.Models.Pages.BranchPage>

@{
    ViewBag.Title = "Listado de Páginas de Sucursales";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewBag.Page = "BranchPageIndex";
}

<div class="portlet light bordered">
    <div class="portlet-title">
        <div class="caption font-dark">
            <i class="icon-settings font-dark"></i>
            <span class="caption-subject bold uppercase"> Listado Páginas de Sucursales </span>
        </div>
    </div>
    <div class="portlet-body">
        <div class="table-toolbar">
            <div class="row">
                <div class="col-md-6">
                    <div class="btn-group">
                        <a href="~/Admin/BranchPage/Edit" class="btn sbold green"><i class="fa fa-plus"></i> Editar Página </a>
                    </div>
                </div>
            </div>
        </div>
        <table class="table table-striped table-bordered table-hover order-column" id="sample_1">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.BranchPageCustomValue)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BranchPageActive)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BranchPageCreatedDate)
                    </th>
                    <th>Vista Previa</th>
                    <th>Activar</th>
                    <th>Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.BranchPageCustomValue)
                        </td>
                        <td>
                            @{
                                if (item.BranchPageActive == true)
                                {
                                    <span class="label label-sm label-success"> Activa </span>
                                }
                                else
                                {
                                    <span class="label label-sm label-warning"> Desactivada </span>

                                }
                            }
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BranchPageCreatedDate)
                        </td>
                        <td>
                            @{ var mEdit = "~/Admin/BranchPage/Preview/" + item.BranchPageId;}
                            <a href="@Url.Content(mEdit)" target="_blank"><li class="glyphicon glyphicon-search " style="color:lawngreen"></li></a>
                        </td>
                        <td>
                            @{
                                var mAct = "~/Admin/BranchPage/SetState/" + item.BranchPageId;
                                if (!item.BranchPageActive)
                                {
                                    <a href="@Url.Content(mAct)"><li class="fa fa-check" style="color:goldenrod">Activar</li></a>
                                }
                            }
                        </td>

                        <td>
                            @if (item.BranchPageActive == false)
                            {
                                var deleteLink = "~/Admin/BranchPage/ConfirmDeletion/" + item.BranchPageId;
                                <a href="@Url.Content(deleteLink)"><div class="glyphicon glyphicon-trash" style="color: goldenrod"> Eliminar</div></a>
                            }
                        </td>

                    </tr>
                                }
            </tbody>
        </table>
    </div>
</div>